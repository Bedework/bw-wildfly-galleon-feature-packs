<?xml version="1.0" ?>
<layer-spec xmlns="urn:jboss:galleon:layer-spec:1.0"
            name="bw-cal-user">
    <!-- Features for personal calendar -->

    <dependencies>
        <layer name="bw-tzserver"/>
        <layer name="bw-logging-rw"/>
    </dependencies>

    <packages>
        <package name="config.user"/>
        <package name="deploy.calendar-xsl-ucalrsrc"/>
        <package name="deploy.images.ucal"/>
        <package name="deploy.war.usercal"/>
        <package name="deploy.webservice.pubcaldav"/>
        <package name="deploy.webservice.publicws"/>
        <package name="deploy.webservice.ucaldav"/>
        <package name="deploy.webservice.userws"/>
    </packages>
</layer-spec>